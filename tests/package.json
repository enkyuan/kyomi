{
  "name": "@kyomi/tests",
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "oxfmt api web",
    "fmt:check": "oxfmt --check api web",
    "lint": "oxlint api web",
    "lint:fix": "oxlint --fix api web",
    "typecheck": "tsc -p api/tsconfig.json --noEmit && tsc -p web/tsconfig.json --noEmit",
    "test": "bun run test:api && bun run test:web",
    "test:api": "bun run test:api:integration",
    "test:api:integration": "cd ../apps/api && bunx --no-install dotenvx run -f ../../docker/.env -f .env -- bun test ../../tests/api/integration",
    "test:api:e2e": "cd ../apps/api && bunx --no-install dotenvx run -f ../../docker/.env -f .env -- bun test ../../tests/api/e2e",
    "test:web": "bun run test:web:integration",
    "test:web:integration": "vitest run --config web/vitest.config.ts",
    "test:web:e2e": "vitest run --config web/vitest.config.ts e2e"
  },
  "devDependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.0",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.4",
    "@kyomi/reader": "workspace:*",
    "@kyomi/ui": "workspace:*",
    "@kyomi/worker": "workspace:*",
    "jsdom": "^28.1.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "typescript": "^5.7.2",
    "vite": "^7.3.1",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.0.5"
  }
}
