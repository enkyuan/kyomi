/** Applied to block images for layout, loading, and reader CSS hooks. */
export const READER_IMG_FRAME = "data-reader-img-frame";
export const READER_INLINE_IMG = "data-reader-inline-img";
/** Applied to author / CMS wrappers so image+text stay side-by-side (see styles.css). */
export const READER_MEDIA_ASIDE = "data-reader-media-aside";
/** Small portrait / avatar: float + max-width (see styles.css). */
export const READER_PROFILE_THUMB = "data-reader-profile-thumb";
/** Marks a text block as an image caption (not inside a `<figure>`). */
export const READER_FIGURE_CAPTION = "data-reader-figure-caption";
/** Marks a text block as a photo/image credit line. */
export const READER_FIGURE_CREDIT = "data-reader-figure-credit";
